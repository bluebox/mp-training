package dev.tulasidhar.lms.DAO;

import java.util.List;

import org.springframework.dao.DataAccessException;

import dev.tulasidhar.lms.model.Book;

public interface BookDao {
	
	public int addBook(Book newBook) throws DataAccessException;
	List<Book> getAllBooks();
	
	int updateBook(Book book);
	int updateBookAvailability(int bookId,boolean isavalable);
	public int addBookLogs(Book book);
	Book getBookById(int bookId);
}
