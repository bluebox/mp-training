import React from 'react';

const Navbar = ({ onNavigate }) => {
    return (
        <nav style={{ padding: '10px', backgroundColor: '#333', color: '#fff' }}>
            <ul style={{ listStyle: 'none', margin: 0, padding: 0, display: 'flex', justifyContent: 'center', gap: '20px' }}>
                <li>
                    <button onClick={() => onNavigate('add')} style={{ color: '#fff', textDecoration: 'none', background: 'none', border: 'none', cursor: 'pointer' }}>Add Book</button>
                </li>
                <li>
                    <button onClick={() => onNavigate('view')} style={{ color: '#fff', textDecoration: 'none', background: 'none', border: 'none', cursor: 'pointer' }}>View Books</button>
                </li>
            </ul>
        </nav>
    );
};

export default Navbar;
