import React, { useState } from 'react';
import './App.css';
import AddUser from './components/AddUser';
import ViewUserRequests from './components/ViewUserRequests';
import Navbar from './components/Navbar';
import MainUsers from './components/MainUsers';


const App = () => {
  const [currentPage, setCurrentPage] = useState('');

  const handleNavigate = (page) => {
     setCurrentPage(page);
   };
  
  const renderPage = () => {
    switch (currentPage) {
      case 'addUser':
        return <AddUser />;
      case 'viewUserRequests':
        return <ViewUserRequests />;
	  case 'MainUsers':
	     return <MainUsers onUpdateClick={(userId) => handleNavigate('RoleManagement')}/>;
	  case 'RoleManagement':
	        return (
				<div>
				<RoleManagement />;
				<GeographicalCoverage />;
				</div>
			);
			
      default:
        return <AddUser />;
    }
  };

  return (
    <div>
      <Navbar onNavigate={handleNavigate} />
      {renderPage()}
    </div>
  );
};

export default App;
