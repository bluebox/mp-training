# some module like random,math,re etc are called built_in modules of python
#that is python provide for their developers
# random ,re,sys etc are the examples
from math import sqrt,pow,pi
from random import randint
from random import random
print(sqrt(36))
print(pi)
print(pow(2,8))
print(randint(1,100))# generate a random int in the provided range
print(int(random()*10))
